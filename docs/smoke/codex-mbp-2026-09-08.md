---
route: codex
backend: codex
host: dev MBP (the owner's development MacBook Pro on the tailnet; phone-sim ran on the same host)
os: macOS 26.6.2 (arm64)
date: 2026-09-08 (run 2026-09-07 17:31–17:33 UTC = 2026-09-08 00:31 host local, UTC+7; the gateway log lines below are in host local time)
layer: A
caty_gateway_version: caty-gateway 0.1.6 from PyPI (isolated prefix UV_TOOL_DIR=~/caty-smoke/uv-tools, Python 3.12.11; the same install as the Claude record on this host)
result: PASS (all seven steps)
---

Owner's decision of 2026-09-08: routes 1, 2 and 6 run on the dev MBP instead of the Mac mini, whose Codex usage window was closed (`codex-mac-mini-2026-09-06.md`, FAIL at step 5 for that reason — that record stays as history and is not superseded, it describes a different host). The MBP's Codex CLI (0.153.4) is logged in; the smoke member is isolated from the owner's own gateway on this host by its own member id, port, launchd label, log file and history directory.

Member `smoke-codex`, port 18782. phone-sim ran on the same host per the "Same host: macOS" block of `README.md` (env from the 0600 launchd plist via the captured extraction command, restart with `launchctl kickstart -k`, log file `~/Library/Logs/caty-gateway-smoke-codex.log`), with `--require-recall --require-restart-observed --require-log-check` and `--turn-timeout 300` (each turn spawns the Codex CLI). It ran concurrently with the Claude smoke on this host (different member, port and label).

No TTS engine is configured for the smoke member, so every turn reports `degraded: "tts"` (text-only fallback; the text reply is intact and layer A passes on it, as `README.md` states).

## Steps

| # | step | result | terminal value |
|---|---|---|---|
| 1 | clean install | PASS | shared with the Claude record on this host: `uv tool install --python 3.12 caty-gateway` → `caty-gateway==0.1.6` |
| 2 | doctor all PASS | PASS | `doctor --backend codex --port 18782`: 14 PASS, 0 FAIL (`codex version`, `codex login` both PASS); the login was also verified with one `codex exec` round trip (`Reply with just OK` → `OK`) |
| 3 | setup / QR issued | PASS | `setup --member smoke-codex --backend codex --yes --port 18782` with `CATY_QR_DELIVERY=tty`: `Setup complete` in 0.9 s, `Setup status: succeeded / Current phase: complete`; launchd label `ai.caty.gateway.smoke-codex` `state = running`; health on loopback answers 401 without a token; ASCII QR shown |
| 4 | pair claim (phone-sim) | PASS | self-issue via `/pair/new` then one `/pair/claim` (same host, tailnet URL): HTTP 200, 0.003 s (`pair_id` b95134be) |
| 5 | turns 1-2 | PASS | turn 1: 200 in 20.7 s, reply `OK` (`gen=9.4s` for the CLI); turn 2: 200 in 21.2 s, reply `調べものや文章作成、アイデアの整理、プログラミングなどをお手伝いできます。`; `degraded: "tts"` on both, see above |
| 6 | restart + turn 3 (resume) | PASS | `launchctl kickstart -k gui/<uid>/ai.caty.gateway.smoke-codex` with `--require-restart-observed`: the new process was listening before any probe failed twice, so `restart.observed: false`, `downtime_s: 0.0`; phone-sim proved the restart by the idle connection it held across it (`proven_by: "connection-drop"`, `sentinel_dropped: true`); launchd pid 73655 → 78921. Turn 3: 200 in 24.7 s, `resume_recall: true` (the codeword from turn 1 came back after the restart — a recall probe, read as `README.md` says: evidence that the conversation continued across the restart, not proof of all history content) |
| 7 | token/pair not in logs | PASS | `--log-file ~/Library/Logs/caty-gateway-smoke-codex.log` → `log_check: pass`; independent check over the 36-line log: `grep -cE '[0-9a-f]{8}\.[0-9a-f]{32}'` = 0, literal search for the member's `CATY_TOKEN` value = 0 |

Wall time of the phone-sim run: 72 s (qr → done), three CLI generations of 9–13 s each.

## phone-sim summary

```
{"claim":{"http_status":200,"latency_s":0.003},"error":null,"finished_at":"2026-09-07T17:32:51Z","gateway_url":"100.104.116.34:18782","label":"codex@mbp","layer":"A","log_check":"pass","log_secret_leak":false,"member_id":"smoke-codex","ok":true,"pair_id":"b95134be","restart":{"downtime_s":0.0,"grace_s":5,"marker_changed":null,"observed":false,"proven_by":"connection-drop","sentinel_dropped":true},"resume_recall":true,"session_id":"smoke-20260907-0080d1","stage":"done","stages":["qr","claim","turn1","turn2","restart","turn3","logcheck","done"],"started_at":"2026-09-07T17:31:39Z","turns":[{"degraded":"tts","http_status":200,"latency_s":20.694,"n":1,"reply_chars":2,"reply_preview":"OK"},{"degraded":"tts","http_status":200,"latency_s":21.198,"n":2,"reply_chars":37,"reply_preview":"調べものや文章作成、アイデアの整理、プログラミングなどをお手伝いできます。"},{"degraded":"tts","http_status":200,"latency_s":24.747,"n":3,"reply_chars":11,"reply_preview":"blue-059f88"}],"warnings":["restart not observed as a health gap; proven by connection-drop"]}
```

(Timestamps are UTC; the gateway log is in host local time.)

## Findings

- Gateway: nothing new. The `codex` backend (CLI spawn per turn), pairing, the recall probe across a `launchctl kickstart -k` restart and log hygiene all PASS on the packaged 0.1.6.
- Observed, not a defect: as on the Claude route, the restart was faster than one probe and was proven by the held connection (#39).
- Observed, not a defect: `degraded: "tts"` on every turn — no TTS engine for the smoke member (text-only fallback, as designed).
- Left on the host: as on the Claude route — `~/caty-smoke/`, the member's plist/log/state files; the service is left running for the owner's layer B session (`caty-gateway qr --member smoke-codex` reissues the QR); to stop: `launchctl bootout gui/<uid>/ai.caty.gateway.smoke-codex`.
